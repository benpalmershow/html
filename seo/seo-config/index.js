/**
 * SEO Configuration Module
 * Unified SEO configuration and meta tag management
 */

export { default as SEOConfig } from '../seo-config.js';

// Initialize SEO configuration
if (typeof window !== 'undefined') {
    // SEO config is initialized when the module is imported
}
